{
  "name": "yaml-include",
  "version": "1.1.1",
  "description": "Custom YAML tag definitions to support modular YAML documents with js-yaml.",
  "main": "index.js",
  "scripts": {
    "test": "multi='spec=- mocha-lcov-reporter=coverage.lcov travis-cov=-' mocha -r blanket --reporter mocha-multi",
    "patch-lcov": "sed -e s,/index,index,g -e s,/dir,lib/dir,g -e s,/file,lib/file,g coverage.lcov | tee coverage.lcov",
    "report-coverage": "npm run patch-lcov && cat coverage.lcov | coveralls && cat coverage.lcov | codeclimate",
    "preversion": "npm test",
    "postversion": "git push && git push --tags"
  },
  "keywords": [
    "yaml",
    "include",
    "modular",
    "js-yaml",
    "js-yaml-tag"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/claylo/yaml-include.git"
  },
  "bugs": {
    "url": "https://github.com/claylo/yaml-include/issues"
  },
  "homepage": "https://github.com/claylo/yaml-include",
  "author": "claylo",
  "license": "ISC",
  "dependencies": {
    "debug": "^2.2.0",
    "js-yaml": "^3.6.1",
    "lodash.merge": "^4.6.0",
    "recursive-readdir-sync": "^1.0.6"
  },
  "devDependencies": {
    "blanket": "^1.1.7",
    "codeclimate-test-reporter": "0.3.3",
    "coveralls": "^2.11.2",
    "mocha": "^3.0.2",
    "mocha-lcov-reporter": "1.2.0",
    "mocha-multi": "^0.9.1",
    "should": "^11.1.0",
    "travis-cov": "^0.2.5"
  },
  "engines": {
    "node": ">=4"
  },
  "config": {
    "blanket": {
      "pattern": [
        "index.js",
        "lib"
      ],
      "data-cover-never": [
        "node_modules"
      ],
      "data-cover-reporter-options": {
        "shortnames": true
      }
    },
    "travis-cov": {
      "threshold": 90
    }
  }
}
